# PostgreSQL connection settings for the HSE Prospective Students backend.
# Copy this file to .env and adjust values to your local database.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=hse_prospective_students
DB_USER=postgres
DB_PASSWORD=postgres